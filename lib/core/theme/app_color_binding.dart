import 'package:clover/core/theme/app_palette.dart';

/// Live palette mirror for legacy `AppColors.token` getters.
abstract final class AppColorBinding {
  static AppPalette palette = AppPalette.light;
}
