# Copy into web/.env.local (gitignored). Never commit real values.
# Also set these in Vercel → Environment Variables (server-only, no NEXT_PUBLIC_).
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_SESSION_SECRET=
