# Admin login uses the Clover account (email + password) with profiles.is_site_admin.
# No ADMIN_SESSION_SECRET required.
# Optional legacy keys (unused):
# ADMIN_EMAIL=
# ADMIN_PASSWORD=
# ADMIN_ALLOW_REGISTER=0
