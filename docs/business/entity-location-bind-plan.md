# План: компания / точка только с местоположением

**Статус:** план + **UI-мок** create-флоу (адрес обязателен визуально; на бэк не уходит)  
**Связано с:** [locations.md](locations.md), [booking-points.md](booking-points.md), [attendance.md](attendance.md), [resources-guide.md](resources-guide.md), [space-plan-resources.md](space-plan-resources.md)

---

## Решение (закреплено)

Сущности **компании** создаются в двух сервисах:

| Сервис | Сущность | Сейчас |
|--------|----------|--------|
| **Запись** (`booking`) | Точка (`booking_points`) | Создаётся без обязательного места |
| **Посещаемость** (`attendance`) | Компания / workplace | Создаётся без обязательного места |

Поля при create (целевой контракт):

| Поле | Обязательность | Смысл |
|------|----------------|--------|
| `location_id` | **Обязательно** (этот план) | Адрес / пин из Ресурсов |
| `space_plan_id` | **Опционально** | Схема зала из Ресурсов — [space-plan-resources.md](space-plan-resources.md); без схемы = только режим «список / как сейчас» |

**Новое правило:** компанию / точку **нельзя создать**, пока не прикрепишь её к **местоположению** из Ресурсов (`locations`).

Схему пространства при create **можно не выбирать** — визуальная бронь появится после привязки published `space_plan`.

Цель — не оставлять пустой адрес: витрина, карта, геозона и (позже) отзывы всегда опираются на место. Схема — отдельный опциональный слой «как выглядит пространство».

### Фильтр постов по местоположению (тоже план)

Рядом с текущими фильтрами постов — **иконка / параметр фильтра по `location`**:

- Посты, у которых есть привязка к местоположению из Ресурсов, можно сузить по выбранному месту (или «все места»).
- Точка входа: тот же паттерн, что у фильтров ленты / витрины — **иконка фильтра**, не отдельный сервис.
- Это **не** фильтр города ленты (страна/город справочника) и **не** категории витрины профиля — отдельное измерение: сохранённое место автора.

**UI (mock/partial):** на профиле рядом с tune-фильтром Ресурсов — иконка места; выбор из своих locations; фильтр сетки на клиенте. RPC `p_location_id` — позже.  
Детали UX на чужом профиле — позже (сейчас sheet: только свой профиль).

---

## Правила

1. **Создать** точку записи или компанию посещаемости → обязательный выбор / создание `location_id`.
2. Местоположения по-прежнему живут в **Ресурсы → Местоположения** ([locations.md](locations.md)).
3. В флоу создания сущности: выбрать существующее место **или** создать новое место и сразу привязать (без отдельного «сначала сходи в Ресурсы» как единственного пути — но место как сущность обязательно).
4. **Удалить место**, на которое ссылается точка / компания → **нельзя**. Сначала перенести сущность на другое место (или отвязать, если продукт позже разрешит — сейчас отвязка без замены **не** планируется).
5. Без ссылок → место удалять можно (как сейчас для постов: неактивное / удаление по RLS владельца — уточнить при реализации).
6. **Схема** (`space_plan_id`) — опционально при create/edit; удаление схемы со ссылками — нельзя (зеркало п.4). Детали: [space-plan-resources.md](space-plan-resources.md).

---

## Что не входит в этот план

- Отзывы / рейтинг после визита — отдельный план: [point-reviews-plan.md](point-reviews-plan.md) (опираются на точку с местом).
- Объединение Записи и Посещаемости в одну «компанию».
- Публичный каталог всех локаций города.

**Сделано (мок):** mobile create-шторка точки записи / компании посещаемости — поле «Адрес *» + mock options (`EntityLocationBindMock`). Create без адреса блокируется snackbar. `location_id` в RPC/таблицы пока не пишется.

---

## Очередь реализации (когда скажем «делать»)

1. Продукт: этот файл + правка `attendance.md` / `booking-points.md` / `locations.md` / `filters.md` (жизненный цикл).
2. Контракт: `docs/supabase/` — `location_id` NOT NULL (или эквивалент) на workplace / booking_points; RLS/RPC гейт создания.
3. Миграция + backfill для уже существующих сущностей без места.
4. Mobile + web: create-флоу с обязательным местом; delete места с проверкой ссылок + UI «перенести на…».
5. Mobile + web: фильтр постов по `location_id` (иконка фильтра) — лента и/или профиль по уточнённому UX.

До явного старта разработки — только этот план.

---

## Связанные документы

- [locations.md](locations.md)
- [booking-points.md](booking-points.md)
- [attendance.md](attendance.md)
- [filters.md](filters.md)
- [features-catalog.md](features-catalog.md)
- [space-plan-resources.md](space-plan-resources.md)
