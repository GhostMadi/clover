# Правила и код (Flutter)

| Что | Где |
|-----|-----|
| **Cursor rules** (архитектура, дизайн, фичи, shared…) | [`rules/`](rules/) |
| Adaptive UI, аудит, справочники enum | [`ui/`](ui/) |
| Ресурсы (цвета, стили, иконки) | [`../../lib/core/resources/README.md`](../../lib/core/resources/README.md) |

Правила в `rules/*.mdc` — **источник истины**.  
В `.cursor/rules/` — симлинки на эти файлы (для Cursor).

Продуктовые сценарии — в [`../business/`](../business/).  
Бэкенд — в [`../supabase/`](../supabase/).
